TYPO3:
  Flow:
    persistence:
      backendOptions:
        # TODO Use attr
        dbname: ssodemoinstance
        host: <%= node['mysql']['bind_address'] %>
        user: root
        password: <%= node['mysql']['server_root_password'] %>

Flowpack:
  SingleSignOn:
    Client:
      client:
        # TODO Use attr
        serviceBaseUri: 'http://ssodemoinstance.vagrant/sso/'
        publicKeyFingerprint: ''
      server:
        DemoServer:
          # TODO Use attr
          serviceBaseUri: 'http://ssodemoserver.vagrant/sso/'
          publicKeyFingerprint: ''

    DemoInstance:
      # TODO Use attr
      demoInstanceName: Vagrant Instance
      # TODO Use attr
      demoServerUri: 'http://ssodemoserver.vagrant/'
